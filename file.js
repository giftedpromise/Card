function toggleMenu() {
  const menu = document.querySelectorAll(".menu-links, menu-links-a");
  const icon = document.querySelectorAll(".hambuger-icon");
}
menu.classList.toggle("open");
icon.classList.toggle("open");
